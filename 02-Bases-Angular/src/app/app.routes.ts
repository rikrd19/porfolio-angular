import { Routes } from '@angular/router';
import { CounterPageComponent } from './pages/counter/counter-pages.component';

export const routes: Routes = [
    {
        path: '',
        component: CounterPageComponent
    }

];
